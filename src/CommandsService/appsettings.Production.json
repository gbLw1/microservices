{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "RabbitMQHost": "rabbitmq-clusterip-srv",
  "RabbitMQPort": 5672,
  "GrpcPlatform": "http://platforms-clusterip-srv:666" // important: use HTTP because we aren't using TLS within the k8s cluster
}
