{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "RabbitMQHost": "rabbitmq-clusterip-srv",
  "RabbitMQPort": 5672,
  "GrpcPlatform": "http://platforms-clusterip-srv:666" // important: use HTTP since our gRPC server is not secured due to our ClusterIP service
}
